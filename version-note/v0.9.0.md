Usando el contexto global de `retrykafka`, implementar `v0.9.0`.

# Objetivo

Agregar MongoDB como IdempotencyStore opcional.

Utilizar:

```text
Unique indexes
Atomic operations
TTL indexes
```

Resolver:

```text
Concurrent claims
Duplicate messages
PROCESSING recovery
Expiration
```

# Testing

Utilizar:

```text
Unit tests
Testcontainers
MongoDB integration
Concurrency tests
Race detector
```

Mantener la interfaz del core estable.

Documentar diferencias de garantías frente a PostgreSQL y DynamoDB.
