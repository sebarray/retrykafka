Usando el contexto global de `retrykafka`, investigar e implementar estrategias avanzadas de ordering.

El comportamiento default debe mantenerse:

```text
No strict ordering across retries.
```

# None

Default:

```text
A fails
B processes
C processes
A retries later
```

Prioriza:

```text
Throughput
Availability
```

# PerPartition

Investigar:

```text
A fails
B waits
C waits
```

Mantener orden por partición.

Analizar:

```text
Poison messages
Head-of-line blocking
Throughput reduction
```

# PerKey

Investigar una estrategia experimental.

```text
key=payment-123

A fails
B waits
C waits

key=payment-999

X continues
Y continues
```

No implementar una solución incorrecta.

Analizar explícitamente:

```text
Distributed consumers
Consumer groups
Rebalances
Crash recovery
Memory limits
Persistent state
Key ownership
```

Si no es posible ofrecer garantías correctas sin infraestructura adicional, documentar la limitación y mantener la feature experimental.

# Testing

Probar:

```text
Ordering guarantees
Concurrent consumers
Rebalances
Poison messages
Crash scenarios
Race detector
```

Documentar claramente el costo de cada estrategia.
