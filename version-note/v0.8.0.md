Usando el contexto global de `retrykafka`, implementar `v0.8.0`.

# Objetivo

Agregar Redis como IdempotencyStore opcional.

Utilizar operaciones atómicas.

Considerar:

```text
SET NX EX
```

Resolver:

```text
Concurrent claims
Duplicate messages
TTL
Lease expiration
Crash recovery
```

Analizar cuidadosamente la diferencia entre:

```text
Redis deduplication
```

y:

```text
Durable transactional idempotency
```

No presentar ambas como equivalentes.

# Testing

Agregar:

```text
Unit tests
Redis integration
Concurrent claims
TTL tests
Race detector
```

Documentar garantías y limitaciones.
