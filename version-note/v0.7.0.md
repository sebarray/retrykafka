Usando el contexto global de `retrykafka`, implementar `v0.7.0`.

# Objetivo

Agregar DynamoDB como adapter opcional.

No modificar el core salvo necesidad real.

# Operaciones

Utilizar:

```text
Conditional Writes
ConditionExpression
attribute_not_exists
TTL
```

Mantener:

```text
PROCESSING
COMPLETED
FAILED
```

Resolver:

```text
Concurrent claims
Duplicate messages
Lease expiration
Crash recovery
```

# Testing

Separar:

```text
Unit
Integration
```

Utilizar DynamoDB Local o estrategia equivalente.

Documentar trade-offs de:

```text
AWS dependency
Cloud semantics
Scalability
TTL behavior
```
